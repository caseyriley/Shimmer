import configureStore from '../store';

// function mapStateToProps(state) {
//     const { userEmail } = state
//     return { email: todos.allIds }
// }

// export default connect(mapStateToProps)(TodoList)